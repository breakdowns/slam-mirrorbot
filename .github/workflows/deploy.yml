name: Manually Deploy to Heroku

on: workflow_dispatch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12
        with:
          heroku_api_key: 3f8a163e-ece7-436d-9c42-4a75978dea63
          heroku_app_name: bokep-mirrorbersama
          heroku_email: uzakimanag.er2@gmail.com
          usedocker: true
          docker_heroku_process_type: web
          stack: "container"
          region: "us"
        env:
          HD_CONFIG_FILE_URL: https://raw.githubusercontent.com/TranCongVinh1/slam-mirrorbot/main/config.env
